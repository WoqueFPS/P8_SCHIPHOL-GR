# TODO - Dark mode across all pages

- [x] Add global dark-mode base styles (background/text/form controls) in `resources/css/app.css` driven by `html.dark`.
- [x] Ensure styles don’t break Tailwind utilities; keep changes minimal and scoped.

- [ ] Build/test the app and verify toggle works on non-landing pages.

